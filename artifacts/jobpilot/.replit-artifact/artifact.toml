kind = "web"
previewPath = "/"
title = "JobPilot"
version = "1.0.0"
id = "artifacts/jobpilot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21845

[services.development]
run = "pnpm --filter @workspace/jobpilot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jobpilot", "run", "build" ]
publicDir = "artifacts/jobpilot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21845"
BASE_PATH = "/"
